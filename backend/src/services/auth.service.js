// ============================================================================
// The Center — Servicio de Autenticación
// Lógica de negocio para registro, login y gestión de tokens JWT
// ============================================================================

// TODO: Implementar
// - hashPassword(password)           → Hashear contraseña con bcryptjs
// - comparePassword(plain, hashed)   → Comparar contraseña
// - generateAccessToken(payload)     → Generar JWT access token
// - generateRefreshToken(payload)    → Generar JWT refresh token
// - verifyToken(token, secret)       → Verificar y decodificar token
// - registerUser(data)               → Crear usuario en BD
// - loginUser(email, password)       → Autenticar y retornar tokens

export default {};
