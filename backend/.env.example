# The Center - Backend
# Copiar a .env y completar valores. No commitear .env

FLASK_ENV=development

# PostgreSQL (obligatorio). Formato: postgresql://usuario:contraseña@host:puerto/nombre_bd
DATABASE_URI=postgresql://postgres:postgres@localhost:5432/the_center

# Obligatorio cuando implementes login (JWT)
# JWT_SECRET=tu-clave-secreta-muy-larga-y-aleatoria
# JWT_EXPIRATION_DAYS=7
